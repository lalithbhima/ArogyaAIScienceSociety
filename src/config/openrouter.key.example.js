// The OpenRouter API key is NOT stored in the website code (GitHub would block it).
// It is stored securely in Google Apps Script instead.
//
// Setup (one time):
// 1. Open script.google.com → your web app project
// 2. Paste the latest google-apps-script/Code.gs → Save → Deploy (new version)
// 3. Run storeOpenRouterApiKey() → paste your key from https://openrouter.ai/keys
//
// Public model/settings live in src/config/openrouter.js
