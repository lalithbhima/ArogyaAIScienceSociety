// This file is typically generated when you initialize Tailwind CSS with `npx tailwindcss init -p`

export default { // Use 'module.exports =' if you are in a CommonJS environment
    plugins: {
      tailwindcss: {}, // Enables Tailwind CSS
      autoprefixer: {}, // Adds vendor prefixes to CSS rules for better browser compatibility
      // You might add other PostCSS plugins here if needed
    },
  }
  